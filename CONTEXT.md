# CONTEXT.md — 领域词汇表

本文件只收录领域术语及其边界，不含实现细节。

## Skills 更新检测

### Update（有更新）

本地与远端内容哈希不一致。确定可用更新，更新按钮可用。

### RemoteDeleted（远端已删除）

**确定性**信号：远端事实不存在。两个子类，处理建议不同：

- **RepoDeleted（仓库已删除）**：仓库级 404/410，整个仓库已删除或转移。来源本身消失。
- **SkillDeleted（技能已删除）**：仓库仍在，但该技能的目录已从远端移除。可能是删除，也可能是改名（Moved）。

RemoteDeleted 状态下"更新"动作不可用（远端无货可拉），由用户手动处理：保留本地副本、卸载、或从其他仓库重装。

### Unreachable（无法检查）

**不确定性**信号：超时、网络错误、403/429 限流、5xx。远端状态**未知**，绝不与 RemoteDeleted 混淆——断网时所有技能不能被报告为"已删除"。

### Moved（改名/迁移）

远端目录消失但以新名称存在。**不自动检测**（无可靠信号），仅在 SkillDeleted 的提示文案中引导用户到发现面板搜索新名称。

## 通用规则

- 确定性/不确定性是本域的核心边界：只有确定性信号（4xx 中的 404/410、目录匹配失败）才允许触发"已删除"表述；一切网络性失败归 Unreachable。
- 检测结果不持久化：状态只在检查时刻成立，随查询缓存过期（5 分钟）失效，下次检查重新判定。
